# API Gateway URL — the frontend sends ALL API requests here.
# The gateway proxies /auth/* → auth-service, /company|jobs|talent|applications/* → core-service, /pipeline/* → ai-service
#
# Local dev: http://localhost:4000
# Production: https://api.internnova.com
VITE_API_GATEWAY_URL=http://localhost:4000
