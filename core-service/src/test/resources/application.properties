# Test configuration — provides the minimum properties needed for
# @WebMvcTest and @SpringBootTest sliced contexts.

jwt.access-secret=test-jwt-secret-for-controller-tests
service.internal-token=test-internal-token
ai.service.base-url=http://localhost:8000

# Cloudinary (dummy values for tests — no real API calls)
cloudinary.cloud_name=test-cloud
cloudinary.api_key=123456789012345
cloudinary.api_secret=test-api-secret-dummy-value
